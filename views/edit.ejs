<% include partials/header %>

<div class="ui main text container segment">
    <div class="ui huge header">Edit <%= blog.title%></div>
    <form class="ui form" action="/blog/<%=blog._id%>?_method=PUT" method="POST" >
        <div class="field">
        <label>Title</label>
        <input type="text" name="blog[title]" value= "<%=blog.title%>" ></input>    
        </div>
        <div class="field">
        <label>Image</label>    
         <input type="text" name="blog[image]" value= "<%=blog.image%>" ></input>   
        </div>
        <div class="field">
        <label>Blog Content</label>
        <textarea name="blog[body]"><%=blog.body%></textarea>    
        </div>
        <button class="ui animated button big blue" tabindex="0">
        <div class="visible content">Submit</div>
        <div class = "hidden content">
            <i class="pointing right icon"></i>
        </div>
        </button>
    </form>
</div>
<!--Instead of using placeholder...you can use value attribute, which pre-fills the input field with something. So we include -->
<!--the blog.title, blog.body, etc.. so it can pre-fill the information from that post to edit it. -->

<!--Since the textarea tag does not have a value attribute, you can just include ejs variable in the space 
before the closing tag-->

<!--Since HTML does not support PUT and DELETE request we need to use something called method override..so in the -->
<!--action field we include the ejs blog.id and after the ejs brackets ?_method=PUT so that express can read that -->
<!--we are using method override and the method is POST. You need to npm install method-override so it works-->

<% include partials/footer %>


